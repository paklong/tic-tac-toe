const gameboard = (function () {

    const gameboard = [
        ['O', 'X', 'O'],
        ['O', 'X', 'O'],
        ['X', 'X', 'O'],
    ];

    //cache dom
    const $cells = document.querySelectorAll('.cell');
    // $cells[0].getAttribute('data-index')

    //render
    function render(params) {

    }
})();